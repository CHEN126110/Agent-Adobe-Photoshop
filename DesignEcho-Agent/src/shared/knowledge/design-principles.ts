/**
 * 通用视觉设计原理（知识底座）—— 给 Agent 打「科班设计师」的设计基本功。
 *
 * 分工：
 * - main-image-framework 讲「主图要传达什么卖点 / 内容策略」（讲什么）。
 * - 本模块讲「怎么把画面做得有构图、有层次、有品质」（怎么做好看），且通用于任何品类/版式。
 * 二者互补：策略决定说什么，原理决定怎么呈现。Agent 之前只有前者，所以产出停在排版。
 *
 * accessibility/image-quality/anti-patterns/decision-priority 四个模块是后补的「可检测底线」层：
 * 前六个模块偏审美判断（构图美不美、色彩搭不搭），这四个模块给的是可核对的客观门槛
 * （WCAG 对比度/间距数值、图像技术缺陷、可枚举反模式、优先级排序规则），二者互补而不重复。
 * WCAG 数值来自 WCAG 2.2 成功标准（1.4.3/1.4.11/1.4.12/2.5.8），非本项目实测，按官方标准直接采信。
 *
 * 双重用途：
 * - 设计依据：Agent 在出视觉方案时按这些原理逐维度思考，用设计语言表达决策。
 * - 评审基准：critic / 设计质量验收据此打分，而不是只看「图层建了没、居中了没」。
 *
 * 红线（与项目一致）：知识只作上下文与评审依据，不直接授权写入，
 * 也不允许把本模块内容硬编码进 Agent 运行时分支。
 */

export type DesignPrincipleFocus =
    | 'overview'
    | 'aesthetic-judgment'
    | 'task-mode'
    | 'composition'
    | 'color'
    | 'hierarchy'
    | 'typography'
    | 'craft'
    | 'accessibility'
    | 'image-quality'
    | 'anti-patterns'
    | 'decision-priority'
    | 'self-check';

const OVERVIEW = [
    '设计不是给元素加装饰，而是围绕交付目的做一组有依据的取舍：让受众看懂什么、比较什么、相信什么、记住什么或准确完成什么。',
    '成熟设计判断要能回答四类问题：① 交付物及使用场景；② 可观察的对象、素材、文档与参考事实；③ 本任务需要建立的视觉关系；④ 用什么证据判断有效。这些是按需补充的判断维度，不是开工表单或固定顺序；信息不足时区分“可自己观察”“可由设计师决定”“只有用户知道”，不要用猜测填事实。',
    '设计方向应先描述信息层级、构图关系、视觉语言和保留关系，再选择 Photoshop 工艺；坐标、效果和工具不是设计方向。',
    '检验一张图是否被设计过：它的视觉选择是否共同服务当前任务，关键内容是否可识别，关系是否清楚，执行是否精确、可编辑且符合交付要求。复杂、炫目或有投影并不天然比克制、扁平或白底更好。'
].join('\n');

const AESTHETIC_JUDGMENT = [
    '审美能力不是一句“做得更好看”，也不是读取一个客观的美丑布尔值。它是设计师借助知识储备与真实感知，对当前任务中的视觉关系作出可解释判断，并把判断转成可验证取舍的能力。',
    '判断必须分四层：① 可观察事实——画面里真实存在什么、在哪里、什么颜色/边界/层级；② 关系诊断——主体与背景、文字与图像、同级与从属、节奏与留白是否形成一致关系；③ 目标关联——这些关系如何支持或妨碍当前受众、媒介、品牌和交付目的；④ 最小行动——保留什么、只改什么、改后用什么新鲜证据复核。事实、判断和动作不得混写。',
    '一次素材候选结果只证明若干图对某个已声明角色的相对适配，不能代替对对象、用途、受众、变体体系和素材角色覆盖的理解。下一次观察应优先消除会改变概念或选材的未知，而不是机械浏览所有文件。',
    '知识储备至少包含：任务与传播目的、构图与视觉心理、信息层级、色彩与材质、字体排印、商品与品类语境、摄影素材角色、Photoshop 合成工艺、平台规格、品牌与用户偏好，以及被人工复核过的正反案例。知识是判断依据，不是固定配方；没有一种构图、比例、白底、阴影或字体能脱离语境永久正确。',
    '参考研究由 Agent 按信息增益决定：面对陌生对象、陌生媒介、缺少可靠视觉基准或只剩泛化风格词时，应主动寻找能回答具体未知的参考；已有充分证据时可以跳过。搜索结果只是候选元数据，必须真实观察后才能形成视觉判断；参考离线或无匹配不构成普通设计写入门禁。',
    '“发现问题”要使用视觉关系语言，不用空泛形容词。不要只说“廉价、普通、不高级”，而要指出可见对象与关系，例如“首屏商品仍保留独立白色矩形背景，与整屏暖灰背景形成无意断层，使商品像临时贴图”；再说明它为何妨碍目标，以及一次最小修正。',
    '“发现美”同样需要证据：某个留白、非对称、低对比、纯白背景或扁平处理若共同强化了识别、节奏、气质或信息关系，就应明确作为 preserve 保留，不能在返工时顺手抹平。审美 Harness 的职责既是发现缺陷，也是保护已经成立的关系。',
    '审美未知不等于失败。未看见最终画面、观察不新鲜、标准不适用或置信度不足时，结论应是待观察/待复核；只有数量、目标、来源、图层关系、导出等有唯一答案且有稳定 proofRef 的事实错误才可形成硬阻断。主观评分只进入质量评价和有界改进，不成为 Photoshop 写权限门禁。',
    '修订应有预算和范围：每次优先处理最影响目标且有可靠诊断的少数问题（通常不超过 3 项），只做一次有界改进并重新观察；禁止把所有低分项、通用 expectedFix 或整套方法论同时注入，避免把已经成立的设计改坏。'
].join('\n');

const TASK_MODE = [
    '先判断任务模式，再使用具体启发式。模式只决定评价重点，不选择 Skill、不授予工具权限，也不把未知自动变成阻塞。',
    '说服型（如营销主图）：优先一眼识别、核心价值、注意力与行动理由；通常需要明确主焦点，但仍服从平台规范和真实产品信息。',
    '比较型（如 SKU 组合模板、规格对照）：优先同级项目的可比性、数量关系、标签映射、重复组件一致性和扫读效率；多个同权焦点是正确结构，不应强造唯一主角。',
    '解释型（如详情页分屏、步骤图、参数图）：优先叙事顺序、证据链、段落节奏和阅读连续性；重点可以随阅读进程逐段转移。',
    '表达型（如海报、KV、社交封面）：优先概念、情绪、品牌记忆与场景适配，同时守住识别和可读性。',
    '规格化生产型（如 SKU 批量出图、白底图导出）：优先组合准确、颜色与来源真实、命名、尺寸、批次一致性和可复核交付；创意只在不破坏规范时介入。',
    '局部修订型：优先保持原设计关系和用户未授权改变的内容，只修目标问题，并用前后读回证明没有意外漂移。',
    '用户说法可能跨模式时（例如“做 SKU”），先观察当前文档、项目素材和用户要求；仍会实质改变交付物时再确认，不要把模板设计、色卡设计与批量生产混成一条流程。'
].join('\n');

const COMPOSITION = [
    '焦点结构取决于任务：说服型常用一个主焦点；比较型需要多个同权单元；解释型需要清晰阅读顺序；规格化生产型需要稳定网格和安全区。不要把单焦点当作所有画面的硬规则。',
    '主体与组件尺寸要从交付场景、缩略图可辨性、项目参考和实际素材测量推导。电商营销图常见的 40%~60% 只能作为某些场景的起点，不能用于 SKU 网格、详情段落或留白型海报的通过判定。',
    '先定义关系再算坐标：画布安全区、列数、组件比例、间距、对齐基准、重复规则、阅读顺序和需要保持的相对关系应当先成立。',
    '版面之所以显得整齐，靠的是**共享的少数几条基准线**和**有限的几档间距**，不是每个元素各自"看起来差不多居中"。把版心按并列单元数分成若干列，让同组元素的左右边缘落在同一批列边界上；元素之间只使用同一套间距档位的几个值，而不是每处都取一个新数值。差几像素的偏移单看不明显，同屏出现多处就是画面"发脏"的主因。',
    '并非所有元素都该入栅：承载信息的文字与重复单元应当共享基准线，主视觉、背景和装饰可以有依据地破格（出血、非对称构图）。破格是有意的选择，不是没对齐的借口——判断标准是它是否服务当前任务。',
    '留白是分组、强调和节奏工具；密度也可以是有意选择。判断标准是是否帮助当前任务，而不是越空越高级或越满越有冲击。',
    '平衡可以对称、非对称或模块化。比较与模板任务中，稳定、重复和可预测通常比刻意制造张力更重要。',
    '有可比参考时先测主体占比、重心、留白与间距，再基于本任务调整；参考是证据，不是照抄对象。放置后必须观察复核，不能只相信计算值。',
    '判断点：当前焦点结构是否匹配任务模式？关键关系是否清楚？缩略图是否可辨？对齐、间距、留白和阅读顺序是否有意且一致？'
].join('\n');

const COLOR = [
    '色彩先服务识别、语义、品牌和产品真实性，再服务情绪。SKU 色卡与商品颜色必须优先保持来源准确，不能为了“更高级”擅自改色。',
    '配色关系应形成可解释的系统：品牌主色、功能色、背景色、文字色和强调色各有职责；同类色、邻近色、互补色只是可选关系。',
    '60-30-10 是部分营销画面的启发式，不是通用配色定律。比较模板、极简信息图、黑白版式或规格化出图完全可以采用不同面积关系。',
    '对比要帮助关键内容被辨认，但不要求每个主体都“跳出来”。同级比较项应保持相近视觉重量；辅助内容可以主动退后。',
    '白色、透明或平台规定的背景可以是正确且有意的设计选择。只有在任务需要品牌氛围或场景表达时，才考虑渐变、色块、纹理和场景背景；不得把复杂背景当成设计完成的证明。',
    '判断点：颜色来源真实吗？色彩职责清楚吗？对比支持当前层级吗？同一批次一致吗？背景选择有任务依据吗？'
].join('\n');

const HIERARCHY = [
    '层级不是固定“三级”，而是任务中的关系结构。营销图可有主次层级；比较图需要同级项目等权、标签从属；详情页需要跨段落层级；生产稿可能只需内容与标识两层。',
    '用大小、明度、色彩、位置、疏密、留白、分组和重复建立关系。差异负责区分，重复负责说明它们属于同一系统。',
    '最重要的内容应优先被看到，但“最重要”可能是一个主体、一组同级选项、一段步骤起点或一个规格标识，不能预设总是产品和卖点。',
    '同时做眯眼测试与扫读测试：前者看整体重量，后者看用户能否按正确顺序找到、比较或核对信息。',
    '判断点：层级是否匹配任务结构？同级与从属关系是否清楚？是否存在无依据的平均用力或过度强调？'
].join('\n');

const TYPOGRAPHY = [
    '字号层级来自信息关系与观看距离。1.6~2 倍只是一种常见起点；同级 SKU 标签应保持同字号同字重，不能为了“层次”破坏可比性。',
    '字重、字号、颜色、位置可以共同区分主次，不必每种手段同时拉满。标题用粗、辅助信息用细是可选策略，不是固定配方。',
    '需要标题与多行文案时，再根据字体、字号、观看距离和项目体系调整字间距与行距；1.3~1.6 倍行距只是常见阅读起点，不适用于所有标签、数字或艺术字。',
    '字体性格要匹配品牌、内容与场景，并优先使用项目已有字体体系。黑体、宋体、圆体没有固定情绪结论，必须结合具体字形和上下文判断。',
    '对齐应服务信息关系：短标题可以居中，多行或长文案通常左对齐更易扫读，同级标签也可以统一居中；同一组信息应遵循共同基准。',
    '判断点：实际存在主从关系时字号/字重是否表达清楚？同级文字是否保持一致？字间距、行距、字体性格与对齐是否服务当前任务？'
].join('\n');

const CRAFT = [
    '工艺服从设计方向。投影、光影、描边、渐变、纹理和景深只有在帮助识别、空间或品牌表达时才使用；扁平、无阴影、纯色同样可以是高质量结果。',
    '产品与素材需要保持可信质感：肌理、光泽、柔软度、颜色和边缘都应尊重来源，不能用效果掩盖低质量素材或错误合成。',
    '需要空间叙事时再处理前景 / 主体 / 背景的纵深；比较模板和规格化生产稿更需要稳定平面关系，不必强造三维空间。',
    '精致度：像素级对齐、间距一致、边缘干净、无溢出错位。再好的构思，粗糙的执行也会毁掉它。',
    '可编辑性也是工艺质量：重复组件、智能对象、图层命名、分组、约束关系和模板槽位应支持后续替换，不能只交一张视觉上暂时正确但不可维护的稿。',
    '整体协调来自规则一致和选择克制，不来自效果数量。判断点：每项工艺是否有目的？边缘、对齐和间距是否准确？结构是否可编辑、可复用、可交付？'
].join('\n');

const ACCESSIBILITY = [
    '文本对比度：正文与背景至少 4.5:1；大号文本（≥18pt 常规或≥14pt 加粗，约等于中文大标题字号）可放宽到 3:1。对比不够不是风格问题，是文字「隐形」的可用性缺陷。',
    '非文本对比：图标、边框、控件等承载理解的图形元素，与相邻背景至少 3:1，否则功能元素会看不清、点不准。',
    '仅当交付物包含网页、App 或其它交互控件时检查触控目标：按钮、可点击热区建议至少 24×24px；静态电商图、海报或 Photoshop 生产稿不适用这条。',
    '不能只靠颜色编码：关键信息（错误提示、状态、分类）不能只用颜色区分，需叠加形状、图标、文字或位置之一，否则色弱/色盲用户读不出。',
    '仅对可重排的数字界面或富文本版式做间距压力测试；固定像素广告图和规格化生产稿应按真实展示尺寸检查可读性、溢出与安全区，不能机械套用网页文本伸缩规则。',
    '判断点：文本/图标对比够不够？交互任务中的触控目标够大吗？关键信息是否只靠颜色区分？当前媒介对应的可用性检查是否通过？'
].join('\n');

const IMAGE_QUALITY = [
    '裁切必须服务任务并保护关键识别部位：白底商品、色卡和规格图通常优先主体完整；特写、海报或社媒画面可以有意裁切以强化信息。无论是否裁切，缩放都必须等比，出现拉伸变形立即回退。',
    '文字避让主体：文字与主体大面积重叠时，优先挪版位、加纯净底板或换布局，不要用阴影/描边去「硬救」可读性。',
    '清晰度与噪点：主体要锐、背景可适度虚化；但锐化过头出现光晕（halo）要回退锐化强度，降噪过头导致材质发「塑料感」要回退降噪强度。',
    '曝光：高光或阴影大面积死白死黑，要先修曝光，不要先堆滤镜掩盖问题。',
    '合成边缘：抠图/合成边缘不能有锯齿、脏边、色彩溢出（色边/halo）等痕迹，这类瑕疵比构图问题更容易让画面显廉价。',
    '判断点：主体完整吗？缩放有没有变形？文字压住主体了吗？有没有锐化光晕/降噪塑料感/曝光死白死黑？合成边缘干净吗？'
].join('\n');

const ANTI_PATTERNS = [
    '过满：版面被塞满而非「有目的的留白」——出现这个信号时先删次要元素，不要把全部文字等比缩小去硬塞。',
    '过花：正文使用彩色、花体、全大写，或同屏字体超过 2~3 种，都是高风险信号，默认应收敛而非「加个性」。',
    '关系错误 / 假强调：需要主从时所有元素被做成同等重量，或需要等权比较时强造主角；反过来「什么都想强调」也说明信息架构有问题，不是强调力度不够。',
    '假居中 / 无关键线：元素看似居中但整体缺乏统一对齐基准；出现对齐混乱要先修对齐，再谈色彩与装饰。',
    '品牌 / 风格漂移：同一批次（同一 SKU 系列、同一详情页多屏）出现多套配色、圆角、阴影语法，属于批次一致性缺陷，不是「每张都有新意」。',
    '装饰掩盖信息：需要强调的东西太多、太杂，本质是内容优先级没排好，不能靠「加更多视觉效果」解决。',
    '自检技巧：删掉一层装饰后画面反而更清楚，可以直接判断原设计存在过度装饰。'
].join('\n');

const DECISION_PRIORITY = [
    '优先级依次是：用户当前指令与真实业务事实 → 交付物目的与平台规范 → 识别/比较/阅读/准确生产等核心任务 → 品牌一致性 → 审美表达与装饰。',
    '设计师可以自主决定有依据、可逆的视觉取舍；只有用户拥有且会实质改变交付物的商业选择才需要询问。能从项目、素材、PSD 或参考观察到的事实应自己查，不要把工作丢回用户。',
    '信息不足时区分影响：未知但可通过真实观察验证的能力或状态应先探测；目标文档、权限、不可逆覆盖等安全事实未知时停止写入；设计质量未知时标记待复核，不能自称通过。',
    '发生冲突时先保证内容真实、关系正确和交付完整，再优化风格。判断点：去掉装饰后，用户是否仍能完成这项交付物要求他完成的判断或行动？'
].join('\n');

const SELF_CHECK = [
    '设计质量自检（也是 critic / 设计验收的评分维度，逐项给「过 / 不过 + 具体短板」）：',
    '① 任务效力：在真实使用尺寸下，受众能否完成本任务要求的识别、点击、比较、理解、记忆或核对？',
    '② 目标视觉化：核心目标是否被视觉关系表达——可能是卖点、比较关系、步骤、规格、情绪或生产一致性，而不只是堆文字？',
    '③ 构图：焦点结构与任务模式匹配吗？关键关系、对齐、间距、平衡、留白和阅读顺序成立吗？',
    '④ 色彩：颜色来源真实、职责清楚、对比可读、品牌与批次一致吗？背景选择是否有依据？',
    '⑤ 层级：同级、从属和阅读顺序是否清楚？有没有错误地平均用力或强造主次？',
    '⑥ 字体：排印层级、可读性、对齐、字体选择和同批一致性过关吗？',
    '⑦ 工艺：主体与边缘完整，合成、对齐、间距、图层组织、智能对象和可编辑性过关吗？',
    '⑧ 整体与交付：所有选择是否共同服务任务，文件、尺寸、命名、版本和实际读回是否完整？',
    '红线：强事实被编造、主体或文字不可辨、颜色/组合/数量错误、关键关系破坏、不可逆误改、未读回却宣称完成，任何一项都不能通过。白底、扁平、对称或极简本身不是失败理由。'
].join('\n');

const SECTIONS: Record<DesignPrincipleFocus, { title: string; content: string }> = {
    overview: { title: '设计 vs 排版：动手前先想清楚', content: OVERVIEW },
    'aesthetic-judgment': { title: '审美判断：知识、观察、诊断与行动', content: AESTHETIC_JUDGMENT },
    'task-mode': { title: '先识别任务模式，再选择设计启发式', content: TASK_MODE },
    composition: { title: '构图原理', content: COMPOSITION },
    color: { title: '色彩原理', content: COLOR },
    hierarchy: { title: '视觉层次原理', content: HIERARCHY },
    typography: { title: '字体排印原理', content: TYPOGRAPHY },
    craft: { title: '品质营造原理', content: CRAFT },
    accessibility: { title: '可用性与可访问性（WCAG 基线）', content: ACCESSIBILITY },
    'image-quality': { title: '图像技术质量', content: IMAGE_QUALITY },
    'anti-patterns': { title: '常见设计反模式检查表', content: ANTI_PATTERNS },
    'decision-priority': { title: '设计决策优先级', content: DECISION_PRIORITY },
    'self-check': { title: '设计质量自检清单（评分基准）', content: SELF_CHECK }
};

export const DESIGN_PRINCIPLE_FOCUS_VALUES: Array<DesignPrincipleFocus | 'all'> = [
    'all', 'overview', 'aesthetic-judgment', 'task-mode', 'composition', 'color', 'hierarchy', 'typography', 'craft',
    'accessibility', 'image-quality', 'anti-patterns', 'decision-priority', 'self-check'
];

/**
 * 按焦点取视觉设计原理；不传或传 'all' 返回全量（带小标题）。
 */
export function buildDesignPrinciplesSummary(focus?: DesignPrincipleFocus | 'all'): string {
    if (focus && focus !== 'all' && SECTIONS[focus]) {
        const section = SECTIONS[focus];
        return `## ${section.title}\n${section.content}`;
    }
    return (Object.keys(SECTIONS) as DesignPrincipleFocus[])
        .map((key) => `## ${SECTIONS[key].title}\n${SECTIONS[key].content}`)
        .join('\n\n');
}

/**
 * 设计质量自检维度（结构化，供 critic / 设计验收按维度逐项打分用）。
 */
export const DESIGN_QUALITY_DIMENSIONS: Array<{ key: string; label: string }> = [
    { key: 'impact', label: '任务效力（识别、点击、比较、理解、记忆或核对）' },
    { key: 'selling_point_visual', label: '目标视觉化（卖点、比较、步骤、规格、情绪或一致性）' },
    { key: 'composition', label: '构图关系（焦点结构、平衡、对齐、间距、留白、顺序）' },
    { key: 'color', label: '色彩（来源真实、职责、对比、品牌与批次一致性）' },
    { key: 'hierarchy', label: '信息关系（同级、从属、分组与阅读顺序）' },
    { key: 'typography', label: '字体排印（层级、可读性、对齐与体系一致性）' },
    { key: 'craft', label: '工艺（素材完整、合成、精致度、图层组织与可编辑性）' },
    { key: 'overall', label: '整体与交付（选择一致、规格、命名、版本与真实读回）' }
];
